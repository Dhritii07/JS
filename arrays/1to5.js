//Extract the first three elements from the array [1, 2, 3, 4, 5] using the slice method.
arr = [1, 2, 3, 4, 5]
arr1 = arr.slice(0,3)
console.log(arr1)

//Use the slice method to extract the last two elements from the array ['a', 'b', 'c', 'd'].
arr = ['a', 'b', 'c', 'd']
arr1 = arr.slice(-2)
console.log(arr1)

//How can you use slice to get a subarray [2, 3] from the array [1, 2, 3, 4, 5]?
arr = [1, 2, 3, 4, 5]
arr1 = arr.slice(1,3)
console.log(arr1)

//Use the slice method to remove the first element from the array ['apple', 'banana', 'cherry'].
arr = ['apple', 'banana', 'cherry']
arr1 = arr.slice(1)
console.log(arr1)

//Extract a subarray ['blue', 'green'] from ['red', 'blue', 'green', 'yellow'] using slice.
arr = ['red', 'blue', 'green', 'yellow']
arr1 = arr.slice(1,3)
console.log(arr1)