//Use the slice method to get a subarray ['orange', 'lemon'] from ['apple', 'orange', 'lemon', 'lime'].
arr = ['apple', 'orange', 'lemon', 'lime']
arr1 = arr.slice(1,3)
console.log(arr1)

//How can you use slice to remove the first three elements from [1, 4, 7, 10, 13]?
arr = [1, 4, 7, 10, 13]
arr1 = arr.slice(3)
console.log(arr1)

//Extract the first four elements from [11, 22, 33, 44, 55, 66] using slice.
arr = [11, 22, 33, 44, 55, 66]
arr1 = arr.slice(0,4)
console.log(arr1)

//Use slice to extract the last element from ['one', 'two', 'three', 'four'].
arr = ['one', 'two', 'three', 'four']
arr1 = arr.slice(-1)
console.log(arr1)

//How can you use slice to extract a subarray [200, 300] from [100, 200, 300, 400]?
arr = [100, 200, 300, 400]
arr1 = arr.slice(1,3)
console.log(arr1)